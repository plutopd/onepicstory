# One Pick Story — 발표자료 디자인 스펙

수집한 레퍼런스 12종을 분석해 정리한 규칙. 다음 덱 제작은 이 문서를 기준으로 한다.

---

## 0. 지난 덱이 실패한 진짜 이유 (진단)

| # | 원인 | 근거 |
|---|---|---|
| **1** | **폰트가 Mac에 없었다** | `build_deck.js`가 전 텍스트에 `Malgun Gothic` 지정. 이건 **Windows 전용**이고 이 Mac에는 설치돼 있지 않음(확인함). PowerPoint가 전부 대체 폰트로 치환 → 의도한 굵기·자간·볼드가 전부 무너짐. **이게 "저게 최종 디자인이야?"의 가장 큰 원인.** |
| **2** | **이미지가 0장** | `grep -c addImage build_deck.js` → **0**. 사진·영상 서비스 덱에 사진이 한 장도 없었음. 레퍼런스 12종 전부 이미지가 화면의 40~70%를 차지. |
| **3** | **표지 대비 미달** | `#F56900` 배경 + 흰 글씨 = **3.04:1**. 큰 제목은 간신히 통과하지만 18pt 부제·12.5pt 푸터는 WCAG AA(4.5:1) **미달**. 눈으로도 흐려 보임. |
| 4 | 카드+숫자원 레이아웃의 반복 | 9장이 거의 같은 골격. 레퍼런스는 슬라이드마다 아키타입을 바꿈. |

---

## 1. 타이포그래피 — 폰트명 함정 주의

설치된 한글 폰트의 **Family명은 `Pretendard` 하나뿐**이다.
`Pretendard Black`, `Pretendard SemiBold` 같은 이름은 **Full Name(스타일명)이지 Family명이 아니다.**
pptx에 스타일명을 적으면 PowerPoint가 해석하지 못해 더 넓은 대체 폰트로 떨어지고,
그 결과 제목이 줄바꿈되고 자간·크기가 전부 무너진다. (2026-09-17에 실제로 발생)

```js
const F = "Pretendard";        // 이것만 사용
// 굵기는 bold: true 로만 표현. "Pretendard Black" 같은 이름 금지.
```

빌드 후 항상 확인:
```bash
python3 -c "import zipfile,re;z=zipfile.ZipFile('OnePickStory_demoday.pptx');print(sorted({f.decode() for f in re.findall(rb'typeface=\"([^\"]+)\"', b''.join(z.read(n) for n in z.namelist() if n.startswith('ppt/slides/slide')))}))"
```
→ `['Pretendard']` 하나만 나와야 한다.

### 스케일 (2026-09-17 개정 — 이전 값은 너무 컸음)

| 용도 | 크기 | 비고 |
|---|---|---|
| 표지 대제목 | 46pt bold | |
| 슬라이드 제목 | 29pt bold | 결론 문장. 핵심 어절만 강조색 |
| 섹션 라벨 | 10.5pt bold | 자간 +2, 강조색, 대문자 |
| 숫자 스탯 | **44pt** bold | 단위는 19pt. 84~92pt는 과했음 |
| 본문 | 12~12.5pt | |
| 캡션·출처 | 10~11pt | |

한글 단위는 띄어 쓴다 — `수백 장`, `0 편`, `15 명`. `%`만 붙여 쓴다.

---

## 1-B. 이전 타이포그래피 메모

 — 이 Mac의 자산을 쓴다

설치 확인된 한글 폰트:

- **Pretendard** — Thin·ExtraLight·Light·Regular·Medium·SemiBold·Bold·ExtraBold·Black (**9웨이트 전부**)
- **Hahmlet** — 9웨이트 (한글 세리프)
- **MaruBuri** — 세리프
- OkDanDan-Bold

> **최종 산출물이 PDF이므로 폰트가 파일에 임베드된다.** 심사위원 PC에 없어도 깨지지 않음 → 커스텀 폰트를 마음껏 써도 안전하다. (pptx 원본을 남에게 넘길 때만 주의)

### 스케일

| 용도 | 폰트 | 크기 | 비고 |
|---|---|---|---|
| 표지 대제목 | **Pretendard Black** | 66–80pt | 자간 -2%, 행간 0.95 |
| 슬라이드 제목 | **Pretendard ExtraBold** | 40–48pt | 2줄 이내, 좌측 정렬 |
| 섹션 라벨 (제목 위) | Pretendard SemiBold | 12pt | 강조색, 자간 +8% |
| 숫자 스탯 | **Pretendard Black** | 80–120pt | 레퍼런스 12종 전부의 공통점 |
| 본문 | Pretendard Regular | 14–16pt | 행간 1.45 |
| 캡션·페이지번호 | Pretendard Medium | 10–11pt | |
| 감성 문구 1회 | **Hahmlet SemiBold** | 28–36pt | 마무리 슬라이드에만 |

한글은 `word-break: keep-all` 대응 — 줄바꿈을 직접 넣어 어절이 쪼개지지 않게 한다.

---

## 2. 컬러 — 측정값 기반으로 확정

```
INK        #111318   본문/제목 (흰 배경 대비 18.58:1)
INK2       #3F4654   보조 본문      (9.48:1)
BRAND      #F56900   앱과 동일한 브랜드 오렌지 — 면·아이콘·강조에만
DEEP       #B23A16   풀블리드 패널 배경 (흰 글씨 5.98:1, 크림 5.64:1) ★
ACCENT_TX  #A83D00   흰/피치 위 오렌지 "글씨"   (6.30:1 / 5.69:1)
CREAM      #FFF7ED   따뜻한 배경 패널
PEACH      #FFF1E6   연한 카드 배경
BUTTER     #FFD166   포인트 (수치 강조, 도형) — 글씨로는 쓰지 말 것
TEAL_TX    #0B6B62   AI 관련 라벨 글씨        (6.38:1)
```

### 확정 규칙

- **`#F56900` 위에 흰 글씨 금지 (3.04:1).** 풀블리드 컬러 슬라이드는 **`#B23A16`**를 쓴다.
- 밝은 오렌지 면을 꼭 쓰고 싶으면 **글씨를 `#111318` 잉크로** (6.11:1).
- 색은 **흰색 70% / 딥 테라코타 20% / 오렌지·버터 10%** 비율. 균등 배분 금지.
- **샌드위치 구조**: 표지·마무리는 딥 컬러, 본문은 흰색.

---

## 3. 모티프 — 하나만 고르고 끝까지 반복

레퍼런스 분석 결과 가장 잘 맞는 건 **Wellspace 방식**:

> **원형으로 크롭한 사진**을 슬라이드 여백에 3~4개, 서로 다른 크기(0.5" / 0.9" / 1.3")로 비대칭 배치. 일부는 슬라이드 밖으로 잘려나가게(bleed).

One Pick Story에 완벽히 맞는다 — 모티프 자체가 "사진"이니까. 컬러바·제목 밑줄 같은 AI 티 나는 장식은 **절대 쓰지 않는다.**

---

## 4. 슬라이드 아키타입 6종 (돌려쓰되 연속 반복 금지)

1. **풀블리드 히어로** — 딥 테라코타 배경, 중앙 대제목, 원형 사진 4개 흩뿌림 → 표지·마무리
2. **하프블리드 사진** — 한쪽 절반이 사진으로 꽉 참(모서리 둥글리기 없음), 반대쪽에 텍스트 → 문제 정의
3. **거대 스탯** — 숫자 하나를 100pt 이상으로, 아래 작은 설명. 2~3개를 높이를 어긋나게 배치 → 증거
4. **UI 조각 플로팅** — 앱 스크린샷을 **통째로 넣지 말고** 의미 있는 부분만 잘라 그림자 얹어 겹치기 (SmartCue 덱 방식) → 기술·해결
5. **모듈 그리드** — 화면을 타일로 쪼개 사진/컬러면/크림면을 번갈아 → 차별점
6. **타임라인·프로세스** — 번호 + 짧은 라벨, 가로 진행 → 편집 파이프라인

### 공통 프레임
- 여백 **0.6"** 고정, 블록 간격 **0.4"** 고정 (섞지 말 것)
- 좌하단 작은 워드마크, 우상단 작은 페이지 번호 `01`~`09` — 전 슬라이드 동일 위치
- 본문은 **좌측 정렬**. 가운데 정렬은 히어로 제목에만.

---

## 5. 이미지 — 가장 중요한 보강

**필요한 것 2종:**

**(A) 제품 UI 조각** — 실제 앱에서 캡처, 통짜 말고 조각으로
- AI가 사진을 골라내는 그리드 (회색으로 빠지는 컷이 보이는 순간)
- 타임라인 + 전환 효과 드롭다운
- 요금제 모달
- 완성된 세로 영상 프레임

**(B) 여행 사진** — 실제 사용 사례. 원형 크롭 모티프와 하프블리드에 쓰임.
→ 부모님 사진은 개인정보라 사용 여부를 반드시 확인받을 것. 안 되면 UI 조각만으로 구성.

**처리 규칙**
- 사진은 **직각 모서리**, 꽉 채움(half-bleed). 둥근 모서리는 원형 크롭에만.
- UI 조각은 흰 배경 위에 **부드러운 그림자**(blur 12, 투명도 70, angle 270 아님 — offset 양수)로 띄운다.
- 사진 위에 글씨를 올릴 땐 어두운 오버레이를 깔아 4.5:1을 확보한다.

---

## 6. 레퍼런스 12종 요약

| 이름 | 배울 점 |
|---|---|
| **Wellspace** (멘탈헬스) ★ | 가장 가까운 모델. 딥컬러+크림, 원형 인물 사진 모티프, 하프블리드 |
| **Tinybeans** ★ | 코랄+흰색, 원·링 도형이 화면 밖으로 잘림, 기기 목업 다중 배치, 제목 끝에 마침표 |
| **Event-agency (Brighter)** | 2색만 사용, 거대 세리프 디스플레이, 사진 콜라주, 얇은 1px 아웃라인 박스 |
| 1 Second Everyday | 사진 그리드가 곧 히어로 비주얼. 수상·평점 배지로 신뢰 증명 |
| SmartCue (SaaS) | UI를 조각내 그림자로 띄우는 법 |
| Digits / CLARIO | 거대 숫자 + 2색 대비, 모듈 그리드 |
| ai-financial / Loopy / NeuroNest / Xpend | 거대 스탯 관습 확인. 단 다크 테크 톤은 이 서비스에 부적합 |
| flowly-beauty | 따뜻한 코랄 + 인물 사진 + 큰 퍼센트 수치 |

원본 이미지: `/private/tmp/claude-501/.../scratchpad/refs/`

---

## 7. 다음 덱 제작 체크리스트

- [ ] 폰트를 **Pretendard**로 전면 교체 (Malgun Gothic 전량 제거)
- [ ] 풀블리드 배경을 `#B23A16`으로 교체, 흰 글씨 대비 재검증
- [ ] 슬라이드당 최소 1개 이미지 — `addImage` 0건 탈출
- [ ] 아키타입 6종을 섞어 배치, 같은 골격 연속 2장 금지
- [ ] 스탯 숫자를 80pt 이상으로
- [ ] 원형 사진 모티프를 전 슬라이드에 일관되게
- [ ] PDF 변환 후 **실제 렌더를 눈으로 확인** (지난번엔 근사치만 보고 넘어갔음)
