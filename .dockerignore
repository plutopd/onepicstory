# Never ship secrets or the local dependency tree into the image.
.env
node_modules
npm-debug.log
.git
.gitignore
.DS_Store
.claude
Dockerfile
.dockerignore

# Presentation artefacts — versioned in git, but the server image has no use
# for them and they would only slow the build.
deck_assets
*.pptx
DECK_DESIGN.md
build_deck.js
image
